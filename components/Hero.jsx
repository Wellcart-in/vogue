// Parallax hero section with CTA
export default function Hero() {
    return (
        <section className="hero">
            <h2>Welcome to My Shop</h2>
            <button>Shop Now</button>
        </section>
    );
}